#To build container
sudo docker build -t lodstats .

#To start container run
CID=$(sudo docker run -t -i -p 8080:5000 --link postgres-lodstats:postgres lodstats)

After that open localhost:8080 in your browser

#Run the command for processing all the files
docker run --volumes-from $CID lodstats /process_all_datasets.sh
