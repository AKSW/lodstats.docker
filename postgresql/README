### The main doc file is available at Docker Hub: https://registry.hub.docker.com/_/postgres/
### Github repo is available at: https://github.com/docker-library/postgres

#Pull the postgres image:
docker pull postgres

#Start a postgres instance:_
docker run --name lodstats-postgres -d postgres

#Connect the application to the postgres:
docker run --name lodstats-instance --link lodstats-postgres:lodstats-postgres-link -d lodstats

#check env variables
sudo docker run --rm --name lodstats-dummy --link lodstats-postgres:lodstats-postgres-link lodstats env
